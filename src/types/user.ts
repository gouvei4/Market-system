export interface IUser {
    id?: string;
    name?: string;
    email?: string;
    active?: boolean;
    phone?: string;
    password?: string;
  }
  
  export interface IUserType {
    id: string;
    name: string;
  }